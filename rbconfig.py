defaultOn2Off = 0.75 #seconds -- hoot on-->off (length of hoot) for most signals
finishOn2Off = 0.25 #seconds -- hoot on-->off (length of hoot) for finishes
defaultSequence = 0 #the signal sequence selected by default
serialRelayPort = '/dev/ttyUSB0' # for Windows, this is probably COM1 (or COM2, COM3 etc)
finshFileUseDefaultFolder = True
finishFileFolder = '/Documents/'
